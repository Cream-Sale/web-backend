package com.creamsale.repository;

import com.creamsale.model.CashBack;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CashBackRepository extends JpaRepository<CashBack, Long> {
}
